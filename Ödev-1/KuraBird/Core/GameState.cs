namespace KuraBird.Core
{
    /// <summary>
    /// Oyun durumlarını temsil eden enum.
    /// </summary>
    public enum GameState
    {
        MainMenu,
        CharacterSelect,
        Playing,
        Paused,
        GameOver,
        HighScores,
        Settings
    }
}
